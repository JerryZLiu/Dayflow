<Page x:Class="Dayflow.Views.TimelineView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Timeline">

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="24,16" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Your Timeline" FontSize="24" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="Today" FontSize="14" Foreground="#666" Margin="16,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Start Recording" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="Settings" Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Timeline Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <!-- Placeholder for timeline cards -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="10:30 AM - 11:15 AM" FontSize="12" Foreground="#666"/>
                        <TextBlock Text="Coding Session" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,2"/>
                        <TextBlock Text="Working on Dayflow Windows migration, implementing core recording and storage features."
                                 FontSize="14" Foreground="#555" TextWrapping="Wrap"/>
                        <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,8,0,0">
                            <TextBlock Text="Coding" FontSize="12" Foreground="#1976D2"/>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="9:00 AM - 10:30 AM" FontSize="12" Foreground="#666"/>
                        <TextBlock Text="Morning Email Review" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,2"/>
                        <TextBlock Text="Reviewed and responded to emails, planned today's tasks."
                                 FontSize="14" Foreground="#555" TextWrapping="Wrap"/>
                        <Border Background="#FFF8E1" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,8,0,0">
                            <TextBlock Text="Email" FontSize="12" Foreground="#FF9800"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Empty state -->
                <Border Style="{StaticResource CardStyle}" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="40">
                        <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        <TextBlock Text="No timeline entries yet" FontSize="18" FontWeight="SemiBold"
                                 HorizontalAlignment="Center" Foreground="#333"/>
                        <TextBlock Text="Start recording to begin building your timeline" FontSize="14"
                                 HorizontalAlignment="Center" Foreground="#666" Margin="0,8,0,16"/>
                        <Button Content="Start Recording" Style="{StaticResource PrimaryButtonStyle}"
                              HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
